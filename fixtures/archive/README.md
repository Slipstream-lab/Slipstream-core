# Archive fixture: `capture.xdr`

**This is a truncated, illustrative capture — NOT real mainnet data.**

It is a single document in the Slipstream capture XDR subset (documented in
`crates/replay/src/xdr.rs`), generated by `tools/gen_capture_fixture.py`, and
used purely to exercise the ledger-archive replay path deterministically:

- Ledger range: `100..=103`
- Checkpoints: `4`
- Transactions: `3`, modelling a sharded-counter workload sharing a `state`
  key (`shard:0`, `shard:1`) — a workload designed to produce measurable
  contention in scheduling, not to resemble any real network capture.

Regenerate with:

```sh
python3 tools/gen_capture_fixture.py
```

Provenance policy: see `fixtures/README.md`. Files that model a live network
must be labelled as illustrative; this one is.
